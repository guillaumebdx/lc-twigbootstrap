{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            {% for sheet in sheets %}
                <div class="col-lg-4 col-md-6 col-sm-12">
                    {% include 'Crook/_card.html.twig' %}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
