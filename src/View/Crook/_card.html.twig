<div class="card">
    <img src="{{ sheet.picture }}" class="card-img-top" alt="logo">
    <div class="card-body">
        <h5 class="card-title">{{ sheet.title }}</h5>
        <p class="card-text">{{ sheet.description }}</p>
        <a href="#" class="btn btn-secondary">Go somewhere</a>
    </div>
</div>
